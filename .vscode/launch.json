{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "name": "Launch Localhost",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}